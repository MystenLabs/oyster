# JSON API Reference

The Oyster JSON API is served under `/api/v1/`. All requests and responses
use JSON. Authenticated endpoints require a Bearer token in the
`Authorization` header.

The following chapters cover each group of endpoints in detail.
